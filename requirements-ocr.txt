# Распознавание таблиц и подписей, вставленных в лист картинкой.
# Ставится отдельно и не обязателен: без него программа работает, только
# такие листы читаются не полностью — она об этом пишет в «Расхождениях».
#
# Движок помечен как несовместимый с Python 3.13 и новее, хотя все его
# зависимости под них есть. Поэтому run.bat при отказе повторяет установку
# с ключом --ignore-requires-python.
rapidocr-onnxruntime>=1.3
